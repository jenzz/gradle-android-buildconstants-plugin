buildscript {
  repositories {
    jcenter()
  }
  dependencies {
    classpath 'com.android.tools.build:gradle:2.0.0-beta5'
  }
}

allprojects {
  repositories {
    jcenter()
  }
}

ext {
  versionNumber = '1.0.0'
}